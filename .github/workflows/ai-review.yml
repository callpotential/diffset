name: AI Code Review
on:
  pull_request:
    types: [opened, synchronize, reopened]
jobs:
  ai-review:
    uses: callpotential/ai-review-workflows/.github/workflows/ai-review.yml@v1
    permissions:
      contents: read
      pull-requests: write
      id-token: write
    secrets: inherit
    